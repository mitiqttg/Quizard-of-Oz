<% layout('./layouts/layout.eta') %>

<h2 class="highlight">Topics for quiz</h2>

<% if (it.allTopics && it.allTopics.length > 0) { %>
    <ul>
    <% it.allTopics.forEach(topic => { %>
        <li> 
            <a href="/quiz/<%= topic.id %>">
                <%= topic.name %>
            </a> 
        </li>
    <% }); %>
    </ul>
    <% if (it.message) { %>
        <p class="message"> <%=it.message%> </p>
    <% } %>
<% } else { %>
    <p>No available topic.</p>
<% } %> 